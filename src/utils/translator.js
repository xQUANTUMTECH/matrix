// La traduzione Google è bypassata: il LLM genera già italiano direttamente dai prompt.
// Qualsiasi chiamata a translate() produce output degradato — non usarla.

export async function handleTranslation(message) {
    return message;
}

export async function handleEnglishTranslation(message) {
    return message;
}
