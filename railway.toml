[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'Xvfb :99 -screen 0 1280x720x24 -ac & export DISPLAY=:99 && node main.js'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[deploy.healthcheck]
path = "/health"
timeout = 30
